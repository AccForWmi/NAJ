function getTodosTemplate(newTaskObj) {
    return "<div class='todos-data'>" +
                "<div class='todos-row'>" +
                    newTaskObj.taskName + 
                "</div>" +
            "</div>";
}
